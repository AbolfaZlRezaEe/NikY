<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/rootViewCartEmptyState"
    android:background="?android:colorBackground"
    android:translationZ="10dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <ImageView
            android:layout_width="@dimen/customCartEmptyStateImageView"
            android:layout_height="@dimen/customCartEmptyStateImageView"
            app:srcCompat="@drawable/ic_cart_empty_state_280dp" />

        <TextView
            android:id="@+id/messageCartEmptyState"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/lowNormalMargin"
            android:textSize="@dimen/textSizeEmptySate"
            android:text="@string/nothingForShowHere"
            android:textAppearance="?textAppearanceHeadline6" />
    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>